---
name: ci
on:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo build
      - run: |
          wget -q "https://example.invalid/tool.tgz"
      - run: |
          tar -xzf tool.tgz
          ./tool/configure
