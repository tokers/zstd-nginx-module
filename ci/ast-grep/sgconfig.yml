# GENERATED by tools/vendor-astgrep-rules.py -- do not hand-edit.
#
# Self-contained ast-grep config for this module. Exists so the pre-commit
# ast-grep hook gates identically in a standalone clone: ast-grep resolves
# sgconfig.yml by walking UP from cwd, and outside /opt/myguard that walk finds
# nothing, leaving the hook scanning against an empty ruleset and reporting
# green while checking nothing.
#
# Refresh with:  tools/vendor-astgrep-rules.py <repo>
# Staleness check (CI-able):  tools/vendor-astgrep-rules.py --check <repo>
#
# ruleDirs resolves relative to THIS FILE's directory, not to the working
# directory ast-grep was invoked from -- so `rules` is correct even when the
# hook calls `ast-grep scan -c ci/ast-grep/sgconfig.yml` from the repo root.
# Verified 2026-08-08: spelling it `ci/ast-grep/rules` instead makes ast-grep
# look for ci/ast-grep/ci/ast-grep/rules and fail.
ruleDirs:
  - rules
